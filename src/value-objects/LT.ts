export type LTJson = {
  date: string;
  eventName: string;
  title: string;
  description: string;
  link: string;
};

export type LT = {
  date: Date;
  eventName: string;
  title: string;
  description: string;
  link: string;
};
