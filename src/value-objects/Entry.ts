export type EntryJson = {
  status: string;
  id: string;
  publish: string;
  subject: string;
  entry: string;
};

export type Entry = {
  status: string;
  id: string;
  publish: Date;
  subject: string;
  entry: string;
};
